---
- name: Configure Vault and inject secrets into K8s
  hosts: localhost
  connection: local
  roles:
    - vault-config

- name: Deploy chatbot stack to Kubernetes
  hosts: localhost
  connection: local
  roles:
    - k8s-deploy
