---
- name: Configure Vault and inject secrets
  hosts: localhost
  connection: local
  roles:
    - vault-config

- name: Deploy chatbot to Kubernetes
  hosts: localhost
  connection: local
  roles:
    - k8s-deploy

# Legacy Docker deploy via k8s_workers omitted for local environment (no workers configured)
