apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: chatbot-prod
  labels:
    app: chatbot
spec:
  selector:
    app: chatbot
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
