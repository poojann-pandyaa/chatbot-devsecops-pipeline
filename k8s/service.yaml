apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: chatbot-prod
  labels:
    app: chatbot-backend
spec:
  selector:
    app: chatbot-backend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP
